import PrimaryBanner from "./PrimaryBanner";

import { FPrimaryBanner } from "./PrimaryBanner/PrimaryBanner.contentful";

const Components = {
  primaryBanner: {
    factory: FPrimaryBanner,
    component: PrimaryBanner
  }
};

export default Components;
